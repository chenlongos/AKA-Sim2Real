# =============================================================================
# AKA-Sim2Real Docker 环境变量
# 复制为 .env: cp .env.docker.example .env
# 以下变量全有默认值（见 backend/config.py），不设也能正常跑
# =============================================================================

# ---- 唯一需要改的 ----
# 生产环境替换为实际域名，多个逗号分隔
CORS_ORIGINS=http://localhost:5175,http://localhost